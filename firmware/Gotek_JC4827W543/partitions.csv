# ESP32-S3 Guition JC4827W543 (N4R8 — 4MB flash / 8MB OPI PSRAM)
# Maximised dual-OTA layout for a 4MB chip: two 1.94MB app slots (SD firmware-update
# writes the inactive slot), no SPIFFS (the panel firmware never uses it), no coredump.
# App slots must start on a 64KB boundary, so this is the largest symmetric pair that fits.
# Sketch-local partitions.csv overrides the IDE Partition Scheme menu:
#   Tools > Flash Size = 4MB, Partition Scheme = Custom
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x5000,
otadata,    data, ota,      0xe000,    0x2000,
app0,       app,  ota_0,    0x10000,   0x1F0000,
app1,       app,  ota_1,    0x200000,  0x1F0000,
